% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution.R
\name{evbs_return_level}
\alias{evbs_return_level}
\title{Return level and expected shortfall for the EVBS regression model}
\usage{
evbs_return_level(object, x, period, es = FALSE)
}
\arguments{
\item{object}{An object of class \code{evbsreg} from \code{\link{evbsreg.fit}}.}

\item{x}{Covariate vector (including the intercept) at which to evaluate.}

\item{period}{Return period, expressed in the units of one observation. For
monthly maxima and a \eqn{T}-year return level, use \code{period = 12 * T}.}

\item{es}{Logical; if \code{TRUE}, also return the expected shortfall.}
}
\value{
A named numeric vector with the return level and, if requested, the
  expected shortfall.
}
\description{
Computes the \code{period}-observation return level and, optionally, the
conditional tail expectation (expected shortfall) at a given linear
predictor value.
}
\examples{
data(itajai)
fit <- evbsreg.fit(cbind(1, itajai$pressure), itajai$wind)
# 50-year return level at mean pressure, from monthly maxima
evbs_return_level(fit, x = c(1, mean(itajai$pressure)),
                  period = 12 * 50, es = TRUE)
}
