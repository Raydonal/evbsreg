% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evbsreg_diagnostics.R
\name{plot_aggregate_contributions}
\alias{plot_aggregate_contributions}
\title{Plot Aggregate Contributions B_j(q)}
\usage{
plot_aggregate_contributions(
  diag,
  q = 7,
  label.flagged = 5,
  pch = 16,
  cex = 0.7,
  main = ""
)
}
\arguments{
\item{diag}{A list returned by \code{\link{cnc_diagnostics}}.}

\item{q}{Integer influence threshold in \eqn{1, \ldots, 7}. The paper
uses \code{q = 7}.}

\item{label.flagged}{Number of largest observations to label.}

\item{pch}{Plotting character (default \code{16}).}

\item{cex}{Point size expansion (default \code{0.7}).}

\item{main}{Plot title (default empty).}
}
\value{
The indices of the labelled observations, returned invisibly.
  Also produces a base-graphics plot as a side effect.
}
\description{
Produces panel (b) of the diagnostic figure: the aggregate contributions
\eqn{B_j(q)} of each observation, with a horizontal reference line at
\eqn{b(q)} and automatic labelling of the most influential points.
}
\examples{
data(itajai)
fit <- evbsreg.fit(cbind(1, itajai$pressure), itajai$wind)
diag <- cnc_diagnostics(fit)
plot_aggregate_contributions(diag, q = 7, main = "(b)")

}
\seealso{
\code{\link{plot_cnc}}, \code{\link{cnc_diagnostics}}.
}
