% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evbsreg_diagnostics.R
\name{plot_cnc}
\alias{plot_cnc}
\title{Combined Conformal Normal Curvature Diagnostic Plot}
\usage{
plot_cnc(diag, q = 7, label.flagged = 5)
}
\arguments{
\item{diag}{A list returned by \code{\link{cnc_diagnostics}}.}

\item{q}{Integer influence threshold in \eqn{1, \ldots, 7}
(default \code{7}).}

\item{label.flagged}{Number of largest observations to label in the
right panel (default \code{5}).}
}
\value{
Called for its side effect (a two-panel base-graphics figure).
  Returns \code{NULL} invisibly.
}
\description{
Produces the two-panel diagnostic figure of the paper: normalized
eigenvalues (left) and aggregate contributions \eqn{B_j(q)} (right),
side by side.
}
\examples{
data(itajai)
fit <- evbsreg.fit(cbind(1, itajai$pressure), itajai$wind)
diag <- cnc_diagnostics(fit)
plot_cnc(diag, q = 7)

}
\seealso{
\code{\link{cnc_diagnostics}},
  \code{\link{plot_normalized_eigenvalues}},
  \code{\link{plot_aggregate_contributions}}.
}
